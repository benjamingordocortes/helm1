apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "miapp.fullname" . }}-test-connection"
  labels:
    {{- include "miapp.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "miapp.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
